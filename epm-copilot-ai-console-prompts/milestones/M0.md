# Milestone M0 — Focused Audit & Embedding Plan (no code changes)

**Goal:** establish exactly where the AI Console can plug in. Documentation only; modifying source files is forbidden.

## Tasks

1. **Frontend reality:** does `apps/web` exist and run? If yes — inventory routes, the admin area, nav/shell components, design-token implementation, auth wiring. If no (Ph-6 never started) — record the decision that **the AI Console becomes the first real screen-set of `apps/web`**, built to the approved 5-screen design patterns and tokens; list what minimal shell (auth, nav, layout) must be built first and confirm it matches prompt-library conventions (prompts 01–03).
2. **Runtime reality:** do `packages/orchestrator` (Ph-4) or `packages/kc-server` (Ph-5) exist in any form? Any MCP client/server code anywhere? Decision: TS Agent Executor placement (inside `apps/api` vs sibling service).
3. **Knowledge reality:** how KC content is stored today (tables, files, components); whether embeddings/vector search exist anywhere (pgvector? LanceDB? nothing?). Decision: index strategy for `knowledge-mcp` (one strategy only).
4. **Provider/keys reality:** the existing `llm_providers` records, credential-reference pattern, region allow-list enforcement point, and `llm_call_records` shape — name the exact integration points the Agent Executor will reuse. Env var names only; never print values.
5. **API & auth reality:** route registration pattern, role-guard middleware, tenant scoping helper, audit-log writer — the exact utilities console routes must reuse.
6. **Service-stub reality:** which of the 71 endpoints' services are implemented vs TODO stubs (matters for regression scope).
7. Produce **`GAP_MAP.md`** (target element → exists? → REUSE/EXTEND/ADD → files → risk) covering every element of CLAUDE.md §3–§5, and **`EMBEDDING_PLAN.md`**: nav placement, feature-flag mechanism, route prefix, migration list, and any re-sequencing of M1–M4 you recommend with reasons.

## Acceptance Checks

- [ ] `git status` clean — zero source modifications.
- [ ] GAP_MAP covers 100% of CLAUDE.md §3–§5 with no TBDs; explicitly answers the apps/web question and the vector-index decision.
- [ ] EMBEDDING_PLAN names the exact files/middleware to reuse for auth, tenant scoping, audit, providers.
- [ ] Any CLAUDE.md assumption contradicted by the repo is flagged.

**STOP — human reviews GAP_MAP.md and EMBEDDING_PLAN.md before M1.**
